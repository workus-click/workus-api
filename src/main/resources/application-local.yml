spring:
  config:
    activate:
      on-profile: local
    import: "optional:file:.env[.properties]"

  datasource:
    url: jdbc:mysql://localhost:3306/workus?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1111
    hikari:
      pool-name: workus-pool
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 30000

  data:
    redis:
      host: ${REDIS_URL}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}

  jpa:
    properties:
      hibernate:
        show_sql: true
        generate_statistics: false

  quartz:
    properties:
      org:
        quartz:
          dataSource:
            quartzDS:
              URL: ${DB_URL}
              user: ${DB_USER}
              password: ${DB_PASSWORD}
              maxConnections: 10

server:
  port: 8080

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    url: /openapi.yaml
    disable-swagger-default-url: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
